# 🏷️ Система версионирования Linux Server Manager

## 📋 Краткое описание
Git-базированная система версионирования с автоматическим увеличением patch версий и ручным управлением major и minor версиями.

## 🎯 Цель документа
Для разработчиков и системных администраторов, которые хотят понять и управлять системой версионирования Linux Server Manager.

---

## 📋 Содержание
- [Обзор системы](#обзор-системы)
- [Автоматическое версионирование](#автоматическое-версионирование)
- [Ручное управление версиями](#ручное-управление-версиями)
- [Автоматические обновления](#автоматические-обновления)
- [Инструменты управления](#инструменты-управления)

---

## 📝 Основное содержание

### Обзор системы

Linux Server Manager использует **Git-базированную систему версионирования** с **автоматическим увеличением patch версий** при каждом коммите и **ручным управлением major и minor версиями**.

#### 🔄 Принципы работы
- **Patch версии** (1.14.17 → 1.14.18) увеличиваются автоматически при каждом коммите
- **Major и Minor версии** управляются вручную через менеджер версий
- **Семантическое версионирование** - MAJOR.MINOR.PATCH формат
- **Автоматическое отображение** версии в веб-интерфейсе

### Автоматическое версионирование

#### 🔄 Автоматическое увеличение patch версии

При каждом коммите Git hook `post-commit` автоматически:
- Получает текущую версию из последнего тега
- Увеличивает patch компонент на 1
- Создает новый тег с обновленной версией

```bash
# Пример автоматического увеличения
git commit -m "Исправление бага"
🔄 Автоматически увеличена версия: v1.14.17 → v1.14.18
```

#### 📊 Форматы времени выполнения

В подвале каждой страницы отображается время выполнения:
```
Linux Server Manager v1.14.18 | © 2025 Roman Segalla | Время генерации: 45.2 ms
```

**Форматы времени:**
- **Микросекунды** (< 1ms): `123 μs`
- **Миллисекунды** (< 1s): `45.2 ms`
- **Секунды** (≥ 1s): `1.234 s`

#### 🛠️ Управление автоматическим версионированием

**Включено по умолчанию** - patch версии увеличиваются автоматически при каждом коммите.

```bash
# Отключить автоматическое увеличение
./disable-auto-patch.sh

# Включить автоматическое увеличение
./enable-auto-patch.sh

# Проверить статус
./check-auto-patch.sh
```

### Ручное управление версиями

#### 🎯 Управление major и minor версиями

Для значимых изменений используйте обычные Git теги (без дополнительных скриптов).

**Получение текущей версии:**
```bash
# Получить текущую версию
git fetch --tags
git describe --tags --abbrev=0 2>/dev/null || echo "v0.0.0"

# Показать детальную информацию
git show --stat $(git describe --tags --abbrev=0)
```

**Создание MINOR версии:**
```bash
# Пример: v1.14.24 → v1.15.0
git tag -a v1.15.0 -m "Minor: v1.15.0 - Добавлены новые функции"
git push origin v1.15.0
```

**Создание MAJOR версии:**
```bash
# Пример: v1.15.3 → v2.0.0
git tag -a v2.0.0 -m "Major: v2.0.0 - Критические изменения"
git push origin v2.0.0
```

#### 📋 Менеджер версий

Используйте менеджер версий для удобного управления:
```bash
./version-manager.sh
```

**Доступные опции:**
1. **Major версия** (1.14.17 → 2.0.0) - критические изменения
2. **Minor версия** (1.14.17 → 1.15.0) - новые функции
3. **Информация о версии** - показать детали текущей версии
4. **История версий** - показать все теги

#### ⚠️ Важные примечания

- После создания нового MAJOR/MINOR тега, следующий коммит автоматически увеличит PATCH
- Все теги должны быть аннотированными (`-a` флаг)
- Сообщения тегов должны быть информативными
- Всегда пушите теги на удаленный репозиторий

### Автоматические обновления

#### 🚀 Настройка cron

Скрипт `auto-update.sh` настроен для автоматических обновлений через cron:

```bash
# Проверить текущие настройки cron
crontab -l

# Добавить задачу (если не настроена)
# Обновление каждую минуту
* * * * * /path/to/linux-server-manager/auto-update.sh >> /path/to/linux-server-manager/logs/auto-update.log 2>&1

# Обновление каждые 5 минут
*/5 * * * * /path/to/linux-server-manager/auto-update.sh >> /path/to/linux-server-manager/logs/auto-update.log 2>&1
```

#### 🔄 Как работает автоматическое обновление

1. **Проверка обновлений** - скрипт проверяет новые коммиты в GitHub
2. **Обновление кода** - загружает последние изменения
3. **Создание серверного тега** - если версия не изменилась, но есть новые коммиты
4. **Логирование** - все действия записываются в лог

#### 📊 Мониторинг обновлений

```bash
# Просмотр логов автоматических обновлений
tail -f /path/to/linux-server-manager/logs/auto-update.log

# Проверка статуса обновлений
./check-update-status.sh

# Ручной запуск обновления
./auto-update.sh
```

### Инструменты управления

#### 🛠️ Доступные скрипты

| Скрипт | Назначение | Использование |
|--------|------------|---------------|
| `version-manager.sh` | Менеджер версий | `./version-manager.sh` |
| `auto-update.sh` | Автоматические обновления | `./auto-update.sh` |
| `disable-auto-patch.sh` | Отключить авто-патч | `./disable-auto-patch.sh` |
| `enable-auto-patch.sh` | Включить авто-патч | `./enable-auto-patch.sh` |
| `check-auto-patch.sh` | Проверить статус | `./check-auto-patch.sh` |

#### 📋 Примеры использования

**Создание новой minor версии:**
```bash
# Запустить менеджер версий
./version-manager.sh

# Выбрать "Minor версия"
# Ввести описание изменений
# Подтвердить создание тега
```

**Проверка статуса версионирования:**
```bash
# Проверить текущую версию
git describe --tags --abbrev=0

# Проверить статус авто-патча
./check-auto-patch.sh

# Просмотреть историю версий
git tag --sort=-version:refname | head -10
```

**Ручное обновление:**
```bash
# Запустить обновление вручную
./auto-update.sh

# Проверить результат
echo "Текущая версия: $(git describe --tags --abbrev=0)"
```

#### 🔧 Конфигурация

**Настройки в файле конфигурации:**
```bash
# Проверить настройки версионирования
cat .git/hooks/post-commit

# Настройки автоматических обновлений
cat auto-update.sh
```

**Переменные окружения:**
```bash
# Настройка путей для логов
export UPDATE_LOG_PATH="/path/to/logs/auto-update.log"

# Настройка интервала обновлений
export UPDATE_INTERVAL="300"  # 5 минут
```

---

## 🔗 Связанные документы
- **[Основная документация](README.md)** - Индекс всех документов
- **[История изменений](CHANGELOG.md)** - Подробная история всех изменений
- **[Заметки о релизах](RELEASE_NOTES.md)** - Детальная информация о каждом релизе
- **[Статус проекта](PROJECT_STATUS.md)** - Что реализовано и что планируется

## 📞 Поддержка
См. [основную документацию](README.md#📞-поддержка).

---

**📅 Последнее обновление**: 2025-01-16  
**🏷️ Версия документа**: 1.0.0  
**📝 Автор**: Команда Linux Server Manager
