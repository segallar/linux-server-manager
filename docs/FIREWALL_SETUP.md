# 🔧 Настройка файрвола для Linux Server Manager

## 📋 Краткое описание
Инструкция по настройке прав доступа к файрволу UFW/iptables для корректной работы модуля управления файрволом в Linux Server Manager.

## 🎯 Цель документа
Для системных администраторов, которые хотят настроить безопасный доступ к файрволу для веб-приложения Linux Server Manager.

---

## 📋 Содержание
- [Проблема](#проблема)
- [Решение](#решение)
- [Безопасность](#безопасность)
- [Диагностика проблем](#диагностика-проблем)
- [Тестирование](#тестирование)

---

## 📝 Основное содержание

### Проблема

Приложение Linux Server Manager не может получить доступ к файрволу UFW/iptables из-за ограничений прав доступа. Веб-сервер работает под пользователем `www-data`, который не имеет прав на выполнение команд файрвола.

#### 🚨 Симптомы проблемы
- Ошибка "Permission denied" при попытке управления файрволом
- Статус файрвола не отображается в веб-интерфейсе
- Невозможность добавления или удаления правил файрвола

### Решение

#### 🔐 Настройка sudo прав

**Метод 1: Использование visudo (рекомендуется)**

```bash
# Откройте файл sudoers для редактирования
sudo visudo

# Добавьте строку в конец файла
www-data ALL=(ALL) NOPASSWD: /usr/sbin/ufw, /sbin/iptables
```

**Метод 2: Альтернативный способ**

```bash
# Добавьте права через echo (требует root)
echo 'www-data ALL=(ALL) NOPASSWD: /usr/sbin/ufw, /sbin/iptables' >> /etc/sudoers
```

#### ✅ Проверка настроек

```bash
# Проверьте, что права добавлены
tail -1 /etc/sudoers

# Протестируйте доступ от имени www-data
sudo -u www-data sudo ufw status

# Проверьте доступ к iptables
sudo -u www-data sudo iptables -L
```

### Безопасность

#### ✅ Что разрешено
- **Чтение статуса** файрвола (ufw status, iptables -L)
- **Управление правилами** (добавление/удаление)
- **Включение/выключение** файрвола
- **Просмотр статистики** (ufw status verbose)

#### 🚫 Что НЕ разрешено
- Выполнение других команд с sudo
- Доступ к системным файлам
- Изменение конфигурации системы
- Выполнение произвольных команд

#### 🛡️ Рекомендации по безопасности

1. **Ограничьте доступ** только необходимыми командами
2. **Мониторьте логи** sudo для подозрительной активности
3. **Регулярно проверяйте** права доступа
4. **Используйте SELinux/AppArmor** для дополнительной защиты
5. **Ведите журнал** всех операций с файрволом

#### 📊 Мониторинг безопасности

```bash
# Настройка логирования sudo
# Добавьте в /etc/sudoers
Defaults logfile=/var/log/sudo.log
Defaults log_host, log_year, logfile=/var/log/sudo.log

# Мониторинг логов
sudo tail -f /var/log/sudo.log | grep www-data
```

### Диагностика проблем

#### 🔍 Проверка прав доступа через API

```bash
# Получить детальную информацию о файрволе
curl -s http://localhost:81/api/firewall/info/detailed

# Получить статистику файрвола
curl -s http://localhost:81/api/firewall/stats

# Проверить статус файрвола
curl -s http://localhost:81/api/firewall/status
```

#### 📋 Логи для диагностики

```bash
# Проверить логи sudo
sudo tail -f /var/log/auth.log | grep sudo

# Проверить логи PHP
sudo tail -f /var/log/php8.3-fpm.log

# Проверить логи веб-сервера
sudo tail -f /var/log/nginx/error.log
# или
sudo tail -f /var/log/apache2/error.log

# Проверить логи приложения
tail -f /var/www/linux-server-manager/logs/app.log
```

#### 🚨 Частые проблемы

**Проблема**: "sudo: a terminal is required"
```bash
# Решение: Добавьте права в sudoers
www-data ALL=(ALL) NOPASSWD: /usr/sbin/ufw, /sbin/iptables
```

**Проблема**: "Permission denied"
```bash
# Решение: Проверьте пути к командам в sudoers
which ufw
which iptables
# Убедитесь, что пути совпадают с указанными в sudoers
```

**Проблема**: "Command not found"
```bash
# Решение: Убедитесь, что ufw/iptables установлены
sudo apt-get install ufw
# или
sudo yum install iptables-services
```

**Проблема**: "Authentication failure"
```bash
# Решение: Проверьте синтаксис sudoers
sudo visudo -c
# Исправьте ошибки, если они есть
```

### Тестирование

#### ✅ Пошаговое тестирование

1. **Откройте страницу файрвола** в приложении
2. **Проверьте статус** - должен показывать "Активен"
3. **Просмотрите правила** - должны отображаться все правила UFW
4. **Попробуйте добавить правило** - должно работать без ошибок
5. **Проверьте удаление правила** - должно работать корректно

#### 🔧 Автоматическое тестирование

```bash
#!/bin/bash
# Скрипт для тестирования доступа к файрволу

echo "🔍 Тестирование доступа к файрволу..."

# Тест 1: Проверка статуса UFW
echo "📊 Тест 1: Проверка статуса UFW"
if sudo -u www-data sudo ufw status > /dev/null 2>&1; then
    echo "✅ UFW статус доступен"
else
    echo "❌ UFW статус недоступен"
fi

# Тест 2: Проверка iptables
echo "📊 Тест 2: Проверка iptables"
if sudo -u www-data sudo iptables -L > /dev/null 2>&1; then
    echo "✅ iptables доступен"
else
    echo "❌ iptables недоступен"
fi

# Тест 3: Проверка API
echo "📊 Тест 3: Проверка API"
if curl -s http://localhost:81/api/firewall/status > /dev/null; then
    echo "✅ API файрвола работает"
else
    echo "❌ API файрвола не работает"
fi

echo "🏁 Тестирование завершено"
```

#### 📊 Ожидаемые результаты

| Функция | Ожидаемый результат |
|---------|-------------------|
| **Статус файрвола** | Отображается корректно |
| **Список правил** | Все правила видны |
| **Добавление правила** | Работает без ошибок |
| **Удаление правила** | Работает без ошибок |
| **Включение/выключение** | Работает корректно |

---

## 🔗 Связанные документы
- **[Основная документация](README.md)** - Индекс всех документов
- **[Развертывание](DEPLOYMENT.md)** - Инструкции по установке
- **[Политика безопасности](SECURITY.md)** - Информация о безопасности
- **[Статус проекта](PROJECT_STATUS.md)** - Что реализовано и что планируется

## 📞 Поддержка
См. [основную документацию](README.md#📞-поддержка).

---

**📅 Последнее обновление**: 2025-01-16  
**🏷️ Версия документа**: 1.0.0  
**📝 Автор**: Команда Linux Server Manager
