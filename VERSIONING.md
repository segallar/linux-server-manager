# 🏷️ Система версионности Linux Server Manager

## Обзор

Приложение использует Git-базированную систему версионности, которая автоматически отображает:
- **Версию** из Git тегов
- **Хеш коммита** для точной идентификации сборки
- **Дату коммита** для отслеживания времени
- **Время генерации страницы** для мониторинга производительности

## Как это работает

### 1. Git теги
Версия извлекается из последнего Git тега:
```bash
git describe --tags --abbrev=0
```

### 2. Автоматическое отображение
В подвале каждой страницы отображается:
```
Linux Server Manager v1.14.12 | © 2025 Roman Segalla | Время генерации: 45.2 ms
```

### 3. Форматы времени
- **Микросекунды** (< 1ms): `123 μs`
- **Миллисекунды** (< 1s): `45.2 ms`
- **Секунды** (≥ 1s): `1.234 s`

## Управление версиями

### 🆕 Новый менеджер версий
Используйте улучшенный скрипт для управления версиями:
```bash
./version-manager.sh
```

Скрипт автоматически:
- ✅ Проверяет связь тега с текущим коммитом
- ✅ Предотвращает создание дублирующихся тегов
- ✅ Валидирует версии
- ✅ Предлагает семантическое версионирование

### Создание нового тега
```bash
./version-manager.sh
```

Скрипт предлагает:
1. **Patch** - исправления (1.14.12 → 1.14.13)
2. **Minor** - новые функции (1.14.12 → 1.15.0)
3. **Major** - критические изменения (1.14.12 → 2.0.0)
4. **Ручной ввод** - произвольная версия

### Пример использования
```bash
$ ./version-manager.sh
🏷️ Менеджер версий Linux Server Manager
==============================================
📋 Текущая версия: v1.14.12
✅ Текущий тег v1.14.12 связан с HEAD

🎯 Выберите действие:
1) Создать новую версию
2) Показать информацию о текущей версии
3) Отмена

Выберите вариант (1-3): 1

🎯 Выберите тип обновления:
1) Patch (исправления) - v1.14.13
2) Minor (новые функции) - v1.15.0
3) Major (критические изменения) - v2.0.0
4) Ввести версию вручную
5) Отмена

Выберите вариант (1-5): 1
📝 Создаем тег: v1.14.13
✅ Тег v1.14.13 создан локально
✅ Тег отправлен в удаленный репозиторий
🎉 Тег v1.14.13 успешно создан!
```

## 🔧 Исправления проблем

### Проблема: Дублирующиеся теги
**Симптомы:**
- Множество тегов одной версии
- Неправильное отображение версии в приложении
- Автоматическое создание тегов при каждом push

**Решение:**
1. Отключен автоматический Git hook `pre-push`
2. Создан новый менеджер версий с валидацией
3. Очищены дублирующиеся теги

### Проблема: Тег не связан с HEAD
**Симптомы:**
- `git describe --tags --abbrev=0` показывает старую версию
- Версия в приложении не соответствует коду

**Решение:**
- Новый менеджер проверяет связь тега с текущим коммитом
- Предлагает создать новый тег или переместить существующий

## Техническая реализация

### Классы
- `App\Core\GitVersion` - основной класс для работы с Git
- `src/helpers.php` - функции-помощники для шаблонов

### Функции для шаблонов
```php
getGitVersion()           // Версия (1.14.12)
getGitFullVersion()       // Полная версия (v1.14.12 (abc123 - 2025-01-16))
getGitCommitHash()        // Хеш коммита (abc123)
getGitCommitDate()        // Дата коммита (2025-01-16)
getPageExecutionTime()    // Время генерации (45.2 ms)
```

### Отслеживание времени
- Время засекается в конструкторе `Application`
- Вычисляется в момент вывода страницы
- Отображается в подвале каждой страницы

## Преимущества

### ✅ Автоматическое обновление
- Версия обновляется автоматически при создании тегов
- Не нужно вручную редактировать файлы версий

### ✅ Точная идентификация
- Хеш коммита позволяет точно определить сборку
- Дата коммита показывает время последнего обновления

### ✅ Мониторинг производительности
- Время генерации помогает отслеживать производительность
- Позволяет выявлять медленные страницы

### ✅ Простота использования
- Один скрипт для создания тегов
- Автоматическое отображение во всех страницах

### ✅ Надежность
- Проверка связей тегов с коммитами
- Предотвращение дублирующихся тегов
- Валидация версий

## Рекомендации

### Создание тегов
- Используйте `./version-manager.sh` для создания тегов
- Создавайте теги для каждого релиза
- Используйте семантическое версионирование
- Добавляйте описательные сообщения к тегам

### Мониторинг
- Следите за временем генерации страниц
- Расследуйте медленные страницы (> 1 секунды)
- Используйте хеши коммитов для отладки

### Разработка
- Версия автоматически отображается в development
- Используйте функции-помощники в новых шаблонах
- Добавляйте новые метрики при необходимости

## Миграция со старой системы

### Если у вас есть дублирующиеся теги:
```bash
# Показать все теги
git tag --sort=-version:refname

# Удалить дублирующиеся теги (осторожно!)
git tag | grep "v1.14" | grep -v "v1.14.12" | xargs git tag -d

# Удалить теги из удаленного репозитория
git push origin :refs/tags/v1.14.1 :refs/tags/v1.14.2
```

### Отключение автоматического версионирования:
```bash
# Переименовать Git hook
mv .git/hooks/pre-push .git/hooks/pre-push.disabled
```

## Текущее состояние

- **Текущая версия**: v1.14.12
- **Статус**: ✅ Корректная
- **Связан с HEAD**: ✅ Да
- **Автоматическое версионирование**: ❌ Отключено
- **Менеджер версий**: ✅ Активен
